# KI-Nutzungsprotokoll zur Studienarbeit

## Angaben zur Arbeit

- Gruppenname: Mudkip
- Titel der Studienarbeit: Mira
- KI genutzt: ja
- Verwendete KI-Werkzeuge: Claude, ChatGPT 

Wenn keine KI genutzt wurde, reicht hier die Angabe "nein". In diesem Fall müssen die folgenden Abschnitte nicht ausgefüllt werden.

## Kurz-Erklärung

Dieses Protokoll wird als Markdown-Datei im Git-Repository der Gruppe geführt.
Wesentliche KI-Nutzungen werden hier kurz und zeitnah dokumentiert.
Die Nachvollziehbarkeit über Versionen ergibt sich aus der Git-Historie dieser Datei.

Für diese Studienarbeit wurden KI-Werkzeuge als Unterstützung verwendet.
Die wesentlichen Nutzungen sind unten dokumentiert.
Alle übernommenen Inhalte wurden fachlich geprüft, bei Bedarf angepasst und in die Arbeit eigenverantwortlich integriert.

## Übersicht der KI-Nutzung

Tragen Sie hier die wesentlichen Nutzungen ein.
Wenn ähnliche Nutzungen in engem Zusammenhang stehen, können Sie sie zusammenfassen.
Pflegen Sie das Protokoll möglichst zeitnah, damit die Git-Historie die Entwicklung nachvollziehbar macht. Nutzen Sie KI, um Ihren Promtverlauf entsprechnd dieser Vorlage festzuhalten.  

| Datum | Anwender der KI | Werkzeug | Nutzung kurz beschrieben | Übernahme und Anpassung kurz beschrieben |
| --- | --- | --- | --- | --- |
| 2026-04-20 | Marija Voloder | ChatGPT | Farbpalette aus dem Figma-Styleguide auf Kontrastverhältnisse und Barrierefreiheit geprüft. | Als Referenzcheck genutzt; Farbentscheidungen wurden eigenständig getroffen und durch das Feedback bestätigt. |
| 2026-04-20 | Marija Voloder | ChatGPT | Schriftkombination und typografische Skalierung (Schriftart, Gewichtung, Größen) auf Lesbarkeit geprüft. | Schriften wurden selbst ausgewählt; KI-Feedback hat die Eignung der Wahl bestätigt. |
| 2026-04-25 | Marija Voloder | ChatGPT | Platzhaltertexte und Beispielbilder für UI-Mockup-Screens generiert. | Texte wurden übernommen und in die Mockups integriert. |
| 2026-05-05 | Marija Voloder | ChatGPT | Design-Tokens als CSS Custom Properties in `globals.css` auf Basis des selbst erstellten Styleguides generiert. | Token-Struktur und Benennung wurden selbst vorgegeben; KI hat fehlende Werte ergänzt und die Datei konsistent formatiert. |
| 2026-05-05 | Marija Voloder | ChatGPT | Einrichtung von TanStack Router (dateibasiertes Routing), als neu eingesetzte Bibliothek wenig vertraut damit. | Als Einstiegshilfe verwendet; Konfiguration und Routenstruktur wurden an das Projekt angepasst. |
| 2026-05-05 | Marija Voloder | Claude | Unterstützung bei der Strukturierung der `Button`-Komponente mit Varianten, Größen und Icon-Unterstützung. | Vorschläge geprüft und integriert; visuelle Gestaltung und Komponentenlogik selbst festgelegt. |
| 2026-05-05 | Marija Voloder | Claude | Unterstützung beim Schreiben der Unit-Tests für Input- und Textarea-Komponenten. | Tests geprüft und an Props und Validierungsverhalten angepasst. |
| 2026-05-06 | Marija Voloder | Claude | Unterstützung beim Entwurf des `AccessibilityProvider`: React Context mit localStorage-Persistenz und Synchronisierung von `prefers-reduced-motion` auf HTML-Datenattribute. | Kontextstruktur und localStorage-Logik übernommen; Media-Query-Fallback und Datenattribute geprüft und eingebaut. |
| 2026-05-06 | Marija Voloder | Claude | Hilfe beim Aufbau der `AccessibilityPanel`-UI mit Radix UI Popover und Switch, verbunden mit dem Provider. | Komponentenstruktur und ARIA-Setup als Grundlage genutzt; Beschriftungen und Styling selbst festgelegt. |
| 2026-05-07 | Marija Voloder | Claude | Radix UI entfernt; KI nach ARIA-Mustern für eigene barrierefreie Switch- und Popover-Komponenten gefragt. | ARIA-Attributstruktur als Referenz genutzt; Animation und Styling separat umgesetzt. |
| 2026-05-07 | Marija Voloder | Claude | Hilfe beim Entwurf des `getPageItems`-Algorithmus: Berechnung, welche Seitenzahlen und Ellipsis-Marker basierend auf aktueller Seite, Gesamtseitenanzahl und Sibling-Count angezeigt werden. | Algorithmus-Logik übernommen; Styling und ARIA-Labels der Komponente separat umgesetzt. |
| 2026-05-08 | Marija Voloder | Claude | Accessibility-State auf einen Zustand-Store mit `persist`-Middleware migriert; KI zum Einstieg in die Bibliothek genutzt, da neu. | Store-Struktur und Persist-Konfiguration übernommen; Einbindung ins Panel mit KI-Unterstützung umgesetzt. |
| 2026-05-10 | Marija Voloder | ChatGPT | Gefragt, wie Test- und Coverage-Berichte in der GitLab-CI-Pipeline eingebunden werden. | Coverage-Konfiguration in die bestehende Pipeline integriert und an das Vitest-Setup des Projekts angepasst. |
| 2026-05-11 | Marija Voloder | Claude | Hilfe beim Hinzufügen eines barrierefreien Fehlerzustands zur `Input`-Komponente mit Fehlermeldung unterhalb des Felds und korrekter ARIA-Verknüpfung. | Error-Prop und `aria-describedby`-Muster übernommen; visuelle Gestaltung angepasst. |
| 2026-05-11 | Marija Voloder | Claude | Unterstützung beim Einbinden von Live-Validierungslogik im Styleguide für Benutzername- und Titelfelder. | Validierungsansatz übernommen und an die bestehende Input-Komponenten-API angepasst. |
| 2026-05-16 | Marija Voloder | Claude | Unterstützung beim Aufbau des Zustand-Auth-Stores: Token-State und Benutzerprofil in getrennte Slices aufgeteilt, JWT-Decode-Helper zum Auslesen der Claims aus dem Access-Token hinzugefügt. | Store-Struktur übernommen; Auth-Flow auf Basis der API-Spec selbst definiert. |
| 2026-05-16 | Marija Voloder | ChatGPT | Nach dem Muster für einen `authFetch`-Wrapper gefragt, der den Bearer-Token an jede Anfrage hängt und bei 401 automatisch wiederholt. | Wrapper-Muster als Referenz genutzt; an das Fetch-Setup und die QueryClient-Konfiguration des Projekts angepasst. |
| 2026-05-16 | Marija Voloder | Claude | Hilfe beim Aufbau des mehrstufigen Refresh-Flows: Refresh-Cookie gegen Access-Token tauschen, JWT-Claims dekodieren, Account per ID abrufen, dann vollständiges Benutzerprofil laden. | Flow-Logik übernommen und an die Backend-API-Endpunkte angepasst. |
| 2026-05-18 | Marija Voloder | ChatGPT | Gefragt, wie `/v1`-Aufrufe über Vite proxiert werden, um Cookie-Probleme mit `credentials: 'include'` über Origins hinweg zu lösen. | Proxy-Konfiguration in `vite.config.ts` eingebunden; Pfade und Ziel an das Projekt angepasst. |
| 2026-05-18 | Marija Voloder | Claude | Gefragt, wie ein mehrstufiger Registrierungsassistent mit verschachtelten Routen und einem gemeinsamen Layout in TanStack Router aufgebaut wird. | Routing-Ansatz als Ausgangspunkt genutzt; Schrittreihenfolge, Layout und Guard-Logik separat festgelegt. |
| 2026-05-18 | Marija Voloder | ChatGPT | Regex-Muster für Vor- und Nachnamenvalidierung auf Basis der Backend-API-Spec angefragt: deutsche Umlaute, keine Ziffern, keine führenden oder abschließenden Leerzeichen. | Muster geprüft und an die tatsächlichen Feldanforderungen angepasst. |
| 2026-05-18 | Marija Voloder | ChatGPT | Validierungsregeln für deutsche Adressfelder auf Basis der Backend-API-Spec angefragt: Straßenname, Hausnummernformat (z.B. 43a), 5-stellige Postleitzahl, Stadtname. | Regeln geprüft und angepasst; Ziffernfilterung für die Postleitzahl separat hinzugefügt. |
| 2026-05-18 | Marija Voloder | Claude | Gefragt, wie der Schrittstatus (erledigt / aktuell / ausstehend) aus den Benutzerdaten für die Fortschrittsanzeige im Registrierungs-Sidebar berechnet wird. | Logik als Referenz genutzt; Bedingungen an das tatsächliche Benutzerdatenmodell angepasst. |
| 2026-05-18 | Marija Voloder | ChatGPT | Gefragt, wie ein Benutzer beim Aufrufen einer Route automatisch zum ersten unvollständigen Registrierungsschritt weitergeleitet wird. | Weiterleitungslogik als Referenz genutzt; an TanStack Router und die Schritt-Abschlussprüfungen angepasst. |
| 2026-05-18 | Marija Voloder | Claude | Gefragt, wie der `patchUser`-Helper mit typisierter Fehlerbehandlung für Backend-Validierungsantworten aufgebaut wird. | Helperstruktur als Grundlage genutzt; Fehlertyp und Feldzuordnung anhand der API-Spec definiert. |
| 2026-05-19 | Marija Voloder | ChatGPT | Gefragt, wie eine Live-Avatar-Vorschau vor dem Upload mit der FileReader-API umgesetzt wird. | Ansatz als Referenz genutzt; in den Foto-Schritt mit den bestehenden Komponenten eingebunden. |
| 2026-05-19 | Marija Voloder | ChatGPT | Nach dem `onError`-Muster gefragt, um bei einem fehlerhaften Profilbild-URL auf Initialen zurückzufallen. | Muster übernommen und in die Avatar-Komponente eingebunden. |
| 2026-05-19 | Marija Voloder | Claude | Gefragt, wie das Tagline-Feld nur für Anbieter angezeigt und als Pflichtfeld markiert wird, für Kunden aber ausgeblendet bleibt. | Logik als Referenz genutzt; im About-Schritt anhand des Benutzertyps aus dem Auth-Store eingebunden. |
| 2026-05-19 | Marija Voloder | ChatGPT | Nach dem Muster für einen Live-Zeichenzähler in einem Textarea-Feld mit 2000-Zeichen-Limit gefragt. | Muster als Referenz genutzt; im Bio-Feld des About-Schritts eingebunden. |
| 2026-06-04 | Nataliia Shcherba | Claude | „My Services"-Seite für Anbieter implementiert: neue `MyListingCard`-Komponente und Route `/my-listings` mit API-Anbindung an `GET /v1/users/{userId}/listings`, Lade-, Leer- und Fehlerzuständen sowie Weiterleitung über das Nutzermenü. | Komponente und Seitenstruktur auf Basis des Figma-Prototyps und der API-Spec umgesetzt. Button-Stil (gestrichelte Linie, gefülltes Kreissymbol) und Responsivität nach Prototyp-Feedback angepasst. |
| 2026-06-04 | Nataliia Shcherba | Claude | Barrierefreiheit der „My Services"-Seite verbessert: `aria-labelledby` auf dem `article`-Element, spezifische `aria-label`-Attribute für Edit-Buttons, `role="status"` für den Ladezustand, `role="list"` gegen VoiceOver-Regression sowie `aria-live`-Region für die Statistikanzeige. | Maßnahmen direkt aus dem Review übernommen; eigene Prüfung der ARIA-Muster anhand der WCAG-Anforderungen des Projekts. |
| 2026-06-04 | Nataliia Shcherba | Claude | Unit-Tests für `MyListingCard` und `MyListingsPage` mit Vitest und React Testing Library geschrieben: Rendering, ARIA-Struktur, Status-Overlays, Fehlerzustände, API-Aufruf und leere Zustände abgedeckt. | Testmuster an die bestehende Teststrategie des Projekts angepasst; Mock-Strategie für TanStack Router und Navbar selbst festgelegt. |
| 2026-06-04 | Nataliia Shcherba | Claude | „Create New Listing"-Seite (Route `/create-listing`) auf Basis des Figma-Prototyps implementiert: Formular mit Titel, Beschreibung, Stundensatz, Bild-Upload (mit Vorschau), Adressfeldern, Service-Radius-Slider und Tag-Auswahl; Validierung pro Feld, POST `/v1/listings` mit anschließendem Medien-Upload. | Vorgaben schrittweise präzisiert: Anweisung, bestehende Komponenten (`Input`, `Textarea`, `Slider`, `Button`) wiederzuverwenden und alles auf Englisch zu halten. Nicht passende „Delete"/„Hide"-Buttons auf eigenen Hinweis hin entfernt. |
| 2026-06-04 | Nataliia Shcherba | Claude | Tag-Auswahl als Multi-Select-Dropdown umgesetzt: Fehler beim Datenladen analysiert (Backend liefert Array statt `{items}`), ausgewählte Tags als farbcodierte Chips dargestellt (grün für normale, lila für barrierefreie Tags). | Konkretes Feedback gegeben: abgeschnittenes Dropdown bemängelt, Chip-Darstellung und Farblogik vorgegeben; auf meine Nachfrage nach Barrierefreiheit (Tastaturbedienung) statt Eigenbau die Bibliothek Headless UI (`Listbox`) eingesetzt und in eine wiederverwendbare `MultiSelect`-Komponente ausgelagert. |
| 2026-06-04 | Nataliia Shcherba | Claude | Barrierefreiheit der `MultiSelect`-Komponente und des „Create Listing"-Formulars verbessert: `htmlFor`-Verknüpfung des Tags-Labels, `aria-describedby` bei Fehlerzustand, `aria-required`, `role="list"` auf dem Chip-Container, dynamisches `aria-label` mit Auswahlanzahl am Listbox-Button sowie `aria-label` auf den Options-Panel. Headless-UI-Bug umgangen: `aria-describedby` wird von Headless UI intern überschrieben und musste imperativ per Ref gesetzt werden. | ARIA-Muster direkt übernommen; Headless-UI-Einschränkung eigenständig recherchiert und Workaround per `useRef`/`useEffect` eingefügt. |
| 2026-06-04 | Nataliia Shcherba | Claude | Fehler beim Erstellen eines Inserats behoben: Frontend schickte `Content-Type: application/json`, Backend erwartet `multipart/form-data` mit `@RequestPart`. Zwei separate Requests (JSON + Medien) durch einen einzigen Multipart-Request ersetzt. | Fix direkt übernommen; Backend-Signatur aus dem bereitgestellten Controller-Code übernommen. |
| 2026-06-04 | Nataliia Shcherba | Claude | Unit-Tests für `MultiSelect` und `CreateListingPage` mit Vitest und React Testing Library geschrieben: ARIA-Attribute, Chip-Semantik, Validierungsfehler, Speicherzustand, Multipart-Payload und Navigation abgedeckt. | Teststruktur an das bestehende Testmuster des Projekts angepasst; Mock-Strategie für Headless UI und TanStack Router Codesplitting selbst festgelegt. |
| 2026-06-05 | Nataliia Shcherba | Claude | „Edit Listing"-Seite (Route `/edit-listing/$listingId`) implementiert: Formularfelder aus `GET /v1/users/{userId}/listings/{listingId}` vorbelegt, statusabhängige Aktionen (Speichern, Veröffentlichen, Pausieren, Reaktivieren, Löschen), Bildverwaltung mit bestehendem und neuem Upload, inline Löschbestätigung. Bestehende Komponenten (`Input`, `Textarea`, `Slider`, `Button`, `MultiSelect`) vollständig wiederverwendet. | Aufgabe klar mit dem Ziel der Komponentenwiederverwendung formuliert. Fehlende API-Klarheit (fehlende Straße/Hausnummer in der Detailantwort) wurde durch gezieltes Nachfragen in der API-Spec aufgedeckt. KI-Vorschlag, `create-listing.tsx` zu ändern, wurde direkt und eindeutig abgelehnt – die eigene Entscheidung, den bestehenden Multipart-Ansatz beizubehalten, wurde klar kommuniziert. |
| 2026-06-05 | Nataliia Shcherba | Claude | Bildladelogik in der Edit-Seite korrigiert: statt des inline `media`-Felds aus der Detailantwort wird der dedizierte Endpunkt `GET /v1/listings/{listingId}/media` parallel zur Detailabfrage aufgerufen. | Korrekte API-Dokumentation mit allen verfügbaren Medien-Endpunkten nachgeliefert, nachdem die erste Implementierung auf Basis der unvollständigen OpenAPI-Spec die Bilder nicht zeigte. Präzise Fehlerbeschreibung mit Screenshot erleichterte die Diagnose erheblich. |
| 2026-06-06 | Nataliia Shcherba | Claude | `MyListingCard` um farbige Statusbadges erweitert (Active: grün, Draft: grau, Paused: amber, Deleted: rot) und Anzeigelogik für `primaryMedia` bestätigt. Vorschau-Button im Create-Formular auf `secondary`-Stil umgestellt. Lösch-Button auf `accent` (lila) geändert. Zurück-Button auf der Edit-Seite mit Navigation zu `/my-listings` hinzugefügt. | Anforderungen wurden schrittweise und präzise formuliert: konkreter Verweis auf das visuelle Vorbild (Publish-Button) für den Preview-Button, explizite Farbangabe „lila" für den Lösch-Button. Kaputte Bildvorschauen eigenständig als Backend-/Infrastrukturproblem (CDN-Konfiguration) eingeordnet, sodass keine unnötigen Frontend-Änderungen vorgenommen wurden. |
| 2026-06-06 | Nataliia Shcherba | Claude | Unit-Tests für `EditListingPage` mit Vitest und React Testing Library geschrieben: Lade- und Fehlerzustände, Formularvorausfüllung aus der API-Antwort, statusabhängige Buttons (Save/Publish/Pause/Resume), Rücknavigation, Feldvalidierung, PATCH-Aufruf beim Speichern, Statusaktionen sowie Löschfluss mit Bestätigungsdialog abgedeckt. | Teststruktur und Mock-Strategie an das bestehende Testmuster des Projekts angepasst. `createFileRoute`-Mock um `useParams` erweitert, da die Edit-Seite die Listing-ID aus den Routenparametern liest. URL-basiertes Routing im `mockImplementation` gewählt, um Abhängigkeit von Aufrufzählungen zu vermeiden. |
| 2026-06-06 | Nataliia Shcherba | Claude | ESLint- und TypeScript-Fehler in `create-listing.tsx` und `edit-listing.$listingId.tsx` behoben: `eslint-disable-next-line`-Kommentar von der Komponenten-Export-Zeile zur korrekten `Route`-Export-Zeile verschoben, `throw`-inside-`catch`-Muster durch direktes Fehlersetzen mit frühem `return` ersetzt, fehlendes `await` vor `navigate(...)`-Aufrufen ergänzt. | Fehlerursachen anhand der IDE-Warnungen (ESLint, SonarLint) erklärt und gezielt behoben; IDE-Falschmeldungen (FormEvent deprecated, Codeduplikate) eigenständig als nicht relevant eingestuft und bewusst nicht geändert. |
| 2026-06-06 | Nataliia Shcherba | Claude | Cursor-basierte Paginierung für die „My Services"-Seite implementiert: Vorwärts-/Rückwärtsnavigation über `cursor.next` als `from`-Parameter und einen Frontend-Cursor-Stack für die Rückwärtsnavigation (Previous/Next statt Load-more). Bestehende `Pagination`-Komponente aus dem Styleguide wiederverwendet. Tests für Paginierungslogik ergänzt. | Paginierungsstil (Previous/Next vs. Load-more) aktiv entschieden; Wiederverwendung der vorhandenen `Pagination`-Komponente auf eigenen Hinweis hin eingefordert. Instabiles Testverhalten (Auth-Store erzeugt bei jedem Render neues Objekt, löst `useEffect` mehrfach aus) erkannt und durch URL-basiertes Mock-Routing behoben. |
| 2026-06-08 | Nataliia Shcherba | Claude | Statusfilter für die „My Services"-Seite implementiert: Pill-Buttons (All / Active / Draft / Paused / Deleted) übergeben `publicationStatus` als Query-Parameter an `GET /v1/users/{userId}/listings`; Paginierung wird bei Filterwechsel zurückgesetzt; leerer Zustand passt sich je nach aktivem Filter an. | Anforderung mit den konkreten Backend-Endpunkten formuliert; Pill-Stil und Farbgebung sowie filterabhängigen Leertext selbst festgelegt. |
| 2026-06-08 | Nataliia Shcherba | Claude | Unit-Tests für den Statusfilter der „My Services"-Seite geschrieben: Rendering aller fünf Pill-Buttons, Standardauswahl „All" per `aria-pressed`, korrekte URL-Parameter pro Filter, Toggleverhalten der `aria-pressed`-Attribute, Paginierungsreset bei Filterwechsel, filterspezifischer Leertext sowie generischer Leertext mit zwei „Create service"-Buttons. Drei zunächst fehlschlagende Tests (falsche Aufrufzählung nach `mockClear`, doppelter Button-Match) analysiert und robustere Assertions ohne `mockClear` bzw. mit `getAllByRole` umgeschrieben. | Testfälle und Fehlerursachen eigenständig analysiert; Entscheidung, `toHaveBeenCalledWith` statt Aufrufzählung zu verwenden, selbst getroffen. |
| 2026-06-08 | Nataliia Shcherba | Claude | Unit-Tests für `Slider` geschrieben (16 Tests): Rendering, `label` als `aria-label`, ARIA-Attribute (`valuemin/max/now/text`), `defaultValue`-Fallback auf `min`, `disabled`-Zustand, unkontrollierter und kontrollierter Modus, `onChangeCommitted` bei `mouseup` und Pfeiltasten, kein Aufruf bei anderen Tasten, `aria-describedby` mit und ohne `hint`. `EditListingPage`-Tests um 10 weitere Fälle erweitert: Validierung (Beschreibung zu kurz, negativer Preis, ungültige Postleitzahl), Location-Objekt im PATCH-Body (vorhanden/fehlend je nach Adressfeldern), DELETED-Status (deaktivierte Felder, fehlende Buttons, versteckter Bildbereich), Bildrendering aus dem Media-Endpunkt, Bild-Entfernen per DELETE sowie Serverfehler bei fehlgeschlagenem Publish und Löschen. `MyListingsPage`-Tests: `url: RequestInfo`-Typfehler in `mockImplementation` behoben. | Testlücken eigenständig analysiert; Ursache des fehlschlagenden Postleitzahl-Tests (Nicht-Ziffern werden im onChange herausgefiltert) selbst erkannt und mit vierstelligem Zahlenwert behoben. |
| 2026-06-08 | Nataliia Shcherba | Claude | Barrierefreiheit der „Edit Listing"-Seite geprüft und verbessert: `label`-Prop im `Slider` war deklariert aber nie verwendet – Range-Input hatte keinen zugänglichen Namen, nun als `aria-label` gesetzt. `aria-live`-Region für „Saving…" war bedingt gerendert und wurde daher nicht vorgelesen – auf immer im DOM befindliches Element mit wechselndem Text umgestellt. Statusbadge erhielt `sr-only`-Präfix „Status:", damit Screenreader den Kontext kennen. „Bild entfernen"-Buttons für vorhandene Bilder hatten alle dasselbe Label – nun eindeutig mit Index oder `altText`. Pflichtfelder (Titel, Beschreibung, Preis) erhielten `aria-required`. Löschbestätigung erhielt `role="alert"`, damit sie beim Erscheinen vorgelesen wird. | KI hat alle Komponenten (Slider, MultiSelect, Input, Label) eigenständig gelesen und einen strukturierten Befund erstellt. Priorisierung (kritisch/mittel/niedrig) und Entscheidung, welche Probleme behoben werden, wurden eigenständig getroffen. |
| 2026-06-08 | Nataliia Shcherba | Claude | Seitenanimationen für „My Services"-, „Create Listing"- und „Edit Listing"-Seite hinzugefügt, die über den bestehenden „Reduce motion"-Toggle im Accessibility-Panel deaktivierbar sind: drei CSS-Keyframe-Animationen (`fade-in-up`, `fade-in`, `scale-in`) als Tailwind-Utility-Klassen in `globals.css` definiert; Seitenheader und Filterleiste mit gestaffeltem `fade-in-up` versehen; Listing-Cards werden beim Laden nacheinander mit zunehmendem Delay eingeblendet; neu hochgeladene Bildvorschauen skalieren beim Erscheinen ein; die Löschbestätigung in der Edit-Seite blendet sich per `fade-in` ein. Da die vorhandene `html[data-reduced-motion="true"]`-CSS-Regel bereits alle `animation-duration` auf 0 setzt, werden die Animationen automatisch deaktiviert, ohne eigene Logik in den Komponenten. | Animationsauswahl (Typ, Dauer, Staffelung) selbst entschieden; KI hat die CSS-Keyframes und Klassenanwendung umgesetzt. Sichergestellt, dass kein separater `reducedMotion`-Check in den Komponenten notwendig ist. |
| 2026-06-08 | Nataliia Shcherba | Claude | Filter-Pills der „My Services"-Seite visuell überarbeitet: ausgewählter Pill dunkel gefüllt, nicht ausgewählte Pills mit sichtbarem Rahmen (Referenz: „My Bookings"-Design). Zählerbadges pro Status über eine einmalige Hintergrundabfrage (`limit=100`) ergänzt. Kurze Beschreibung unter der Überschrift hinzugefügt, bestehende „X active"-Zeile entfernt. | Design-Vorgabe anhand eines Screenshots formuliert; Entscheidung, die Counts über einen separaten Fetch statt einer dedizierten API zu laden, eigenständig getroffen. |
| 2026-06-08 | Nataliia Shcherba | Claude | VLM-Typen und Fallback-Logik für asynchron generierte Bildbeschreibungen vorbereitet: `VlmStatus`-Typ (`PENDING`, `PROCESSING`, `COMPLETED`, `FAILED`) exportiert; `altTextStatus` zu `ExistingImage` und `primaryMedia` hinzugefügt; `easyDescription` und `easyDescriptionStatus` zu `ListingDetails` und `MyListingSummary` ergänzt. `alt`-Attribute und `aria-label` in `MyListingCard` und der Edit-Seite nutzen den generierten Text nur bei Status `COMPLETED`, andernfalls Fallback auf Titel bzw. leeren String. | Entscheidung, vorerst nur Typen und Fallbacks zu implementieren (ohne Polling und ohne UI-Anzeige für `easyDescription`, da noch keine Anzeigeoberfläche existiert), eigenständig getroffen und mit KI besprochen. |
| 2026-06-08 | Nataliia Shcherba | Claude | Polling für VLM-Alttext-Generierung in der Edit-Seite implementiert: asynchrone Schleife ruft `GET /v1/listings/{listingId}/media` alle 4 Sekunden auf, solange mindestens ein Bild `PENDING` oder `PROCESSING` ist; bei Abschluss oder Fehler stoppt die Schleife; beim Verlassen der Seite wird sie per Cleanup-Flag abgebrochen. Spinner-Badge auf ausstehenden Bildern als visuelles Feedback. Fehler im ersten Ansatz (Einzeltimer brach die Kette ab, wenn `hasVlmPending` zwischen Polls gleich blieb) analysiert und durch eine selbststeuernde async-Schleife behoben. | Polling-Strategie (normales GET, kein spezieller Job-Endpunkt) wurde gemeinsam mit dem Backend-Team definiert; Implementierung und Bugfix eigenständig umgesetzt. |
| 2026-06-10 | Frederik Schwarz | Claude | Unterstützung beim Schreiben der Unit-Tests für `CalendarPage`, `WeeklyScheduleModal` und `ExceptionModal` mit Vitest und React Testing Library. | Testfälle eigenständig ausgewählt und priorisiert; generierte Tests geprüft, fehlgeschlagene Fälle analysiert und Korrekturen (z.B. fehlender `export` in `CalendarPage`, `QueryClientProvider`-Wrapping) selbst eingebracht. |
| 2026-06-10 | Frederik Schwarz | Claude | Buchungsseite mit Seite-an-Seite-Layout aufgebaut: Kalenderraster links (`flex-[3]`) und Zeitpanel rechts (`flex-[2]`) mit vertikaler Trennlinie; Zeitslots in Morgen- und Nachmittagsgruppen aufgeteilt, 1-Stunden-Intervalle aus freien Fenstern generiert (`generateHourSlots`); verfügbare Stunden pro Tag als Punktereihe (1 bis 3 Punkte) auf dem Kalender visualisiert. | Layout-Struktur, `generateHourSlots`- und `dotCount`-Hilfsfunktionen übernommen; Gruppenbezeichnungen, Icons und Punkteskalierung nach Feedback angepasst. |
| 2026-06-10 | Frederik Schwarz | Claude | Dauerwähler mit Stepper, Standortauswahl mit Icons und Adressanzeige sowie Beschreibungs-Textarea in die Buchungsseite eingebaut; `StepBadge`-Komponente für Abschnittsheader ergänzt. | Komponentenstruktur und `maxDurationForSlot`-Logik übernommen; Icons, Farben der aktiven Standortkarte und Beschriftungen nach Feedback angepasst. |
| 2026-06-10 | Frederik Schwarz | Claude | Inserats-Headerkarte mit grün-lila Verlauf und Anbieterinfos implementiert; Farbtoken (`bg-linen`, `bg-background`) aus `globals.css` eingebunden; Sticky-Footer mit Buchungsübersicht ergänzt. | Gradient-Implementierung und Token-Einbindung übernommen; Kartenform und Abstände nach visuellem Feedback (Screenshot) korrigiert. |
| 2026-06-10 | Frederik Schwarz | Claude | POST `/v1/bookings`-Mutation mit Lade- und Fehlerzustand implementiert; Timezone-Bug analysiert und durch lokale Formatierungshilfen (`toLocalDate`, `toLocalDatetime`) behoben. | Mutationsstruktur und Formatierungsfunktionen übernommen; Fehlerursache (UTC-Verschiebung für UTC+-Nutzer) gemeinsam diagnostiziert und in allen Datumsstellen eingebaut. |
| 2026-06-11 | Frederik | ChatGPT/Codex | Zentrale automatisierte Accessibility-Tests mit `axe-core` für alle wiederverwendbaren Komponenten ergänzt bzw. geprüft: alle Dateien aus `src/components` in `components.a11y.test.tsx` registriert, passende Beispiel-Props und zugängliche Namen ergänzt sowie geöffnete Zustände von Popover-, Menü-, Such- und Accessibility-Panel-Komponenten separat getestet. | Test-Registry und Beispielrenderings wurden fachlich geprüft und an die vorhandenen Komponenten-APIs angepasst. Nicht aussagekräftige Axe-Regeln in JSDOM (`color-contrast`, `region`) bleiben für diese isolierten Komponententests deaktiviert; die A11y-Suite wurde mit `npm run test:a11y` verifiziert. |
| 2026-06-11 | Frederik | ChatGPT/Codex | Fehler in den Accessibility-Tests gemeinsam analysiert: Die Tests fanden geöffnete Popover-Dialoge nicht über ihren zugänglichen Namen, obwohl `AccessibilityPanel` und `UserMenu` bereits `aria-label` an `Popover.Content` übergaben. | Ursache wurde in der eigenen `Popover.Content`-Komponente gefunden: zusätzliche HTML-/ARIA-Attribute wurden nicht an das gerenderte `div role="dialog"` weitergereicht. Fix übernommen, indem `Popover.Content` normale `HTMLAttributes<HTMLDivElement>` akzeptiert und weiterleitet; anschließend mit `npm run test:a11y`, `npm run build`, `npm run lint` und `npm test -- --run` geprüft. |
| 2026-06-11 | Frederik | ChatGPT/Codex | Coverage-Scan durchgeführt, fehlende Testabdeckung identifiziert, passende Tests generiert und anschließend erneut einen Coverage-Scan zur Kontrolle ausgeführt. | Generierte Tests fachlich geprüft, an das vorhandene Vitest-Setup angepasst und die erneute Coverage-Auswertung zur Kontrolle genutzt. |
| 2026-06-11 | Frederik | ChatGPT/Codex | Tests für die `MultiSelect`-Komponente aktualisiert: Required-Zustand wird nicht mehr fälschlich am Button erwartet, sondern als zugängliche Beschreibung am Trigger und als `aria-required` am geöffneten Listbox-Element geprüft. Zusätzlich wurde getestet, dass vorhandene `aria-describedby`-Verknüpfungen erhalten bleiben. | Testanpassungen an das tatsächliche Headless-UI-Verhalten und die WAI-ARIA-Semantik angepasst; Assertions fachlich geprüft und auf die bestehende Vitest-/Testing-Library-Struktur abgestimmt. |
| 2026-06-12/13 | Frederik | ChatGPT | Besprechung möglicher accessibility features und improvements, teilweise mit File-Context (mehrere Chats). | `aria-*`-Ergänzungen des letzten Commits geprüft und übernommen. |
| 2026-06-13 | Frederik Schwarz | Claude | Unterstützung bei `WeeklyScheduleModal.tsx`: GET/PUT-Anbindung des Wochenplans, Checkbox pro Tag mit Zeiteingaben, farbcodierte Zeilen. | Komponentenstruktur als Ausgangspunkt genutzt; Layout, Farben, Schriftgrößen, Beschriftungen und Verhalten anhand eigener Prüfung angepasst. |
| 2026-06-13 | Frederik Schwarz | Claude | Unterstützung bei `ExceptionModal.tsx` Add-Tab: Typauswahl (BLOCKED/AVAILABLE), Datumsfeld, Ganztages-Checkbox, Von/Bis-Zeiteingaben, POST `/v1/users/{userId}/exceptions`. | Grundstruktur übernommen; Design nach eigenem Vergleich mit dem Figma-Mockup korrigiert; Typverhalten angepasst. |
| 2026-06-13 | Frederik Schwarz | Claude | Unterstützung beim Manage-Tab in `ExceptionModal.tsx`: Ausnahmeliste, inline Zeitbearbeitung (PATCH), Löschen mit Bestätigungsschritt. | Logik als Referenz genutzt; UX-Ablauf und Tab-Struktur geprüft und angepasst. |
| 2026-06-13 | Frederik Schwarz | Claude | Unterstützung bei der Überarbeitung der Kalender-Zell-Badges: Positionierung und Unterscheidung von OFF/BLK/+AVAIL, Begrenzung der Buchungsanzeige. | Visuelles Ergebnis im Browser geprüft; Badge-Platzierung, Benennung und Größe anhand eigener Rückmeldung angepasst. |
| 2026-06-13 | Frederik Schwarz | Claude | Unterstützung beim Accessibility-Review über `calendar.tsx`, `WeeklyScheduleModal.tsx`, `ExceptionModal.tsx`: aria-live, Tab-Rollen, aria-pressed, erweiterte aria-labels, role alert, aria-hidden auf Trennzeichen. | ARIA-Vorschläge geprüft; jedes Attribut vor der Einbindung erläutert; Umfang des Reviews durch Nutzerin vorgegeben. |
## Optionale ergänzende Hinweise

Hier können Sie bei Bedarf kurz ergänzen,

- wie Sie mit fehlerhaften KI-Antworten umgegangen sind,
- welche Vorschläge Sie bewusst verworfen haben,
- in welchen Fällen die KI nur als Sparringspartner diente.

## Eigenständigkeit und Verantwortung

Wir bestätigen, dass die KI-Nutzung in dieser Arbeit vollständig und nach bestem Wissen dokumentiert wurde.
Wir übernehmen die Verantwortung für die fachliche Richtigkeit, die Auswahl der übernommenen Inhalte und die gesamte abgegebene Arbeit.

- Datum:
- Gruppenname: Mudkip
